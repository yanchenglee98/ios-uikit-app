import UIKit

enum Constants {
    static let height = UIScreen.main.bounds.height
    static let width = UIScreen.main.bounds.width
    static let photosCellWidth = width
    static let photosCellHeight = photosCellWidth / 1.5
    static let iconSize = width / 6
}
